---
import NavyTexture from './NavyTexture.astro';
import type { Service } from '../lib/sanityQueries';
import { DEFAULT_SERVICES } from '../lib/content';
import { resolveServiceIcon, accentFor } from '../lib/icons';

interface Props {
  services: Service[];
}

const { services } = Astro.props;
const items = services.length > 0 ? services : DEFAULT_SERVICES;
---

<section id="servicios" class="from-accent-700 via-accent to-accent-400 relative overflow-hidden bg-gradient-to-br">
  <NavyTexture glow="#b4530933" glowClass="-bottom-24 -left-24 h-[420px] w-[420px]" />

  <div class="relative mx-auto w-full max-w-7xl px-5 py-20 md:px-9 md:py-24">
    <div class="mb-12 max-w-2xl">
      <div class="text-accent-200 mb-3 text-[13px] font-semibold tracking-[0.08em] uppercase">Servicios</div>
      <h2 class="font-display text-[28px] font-extrabold tracking-tight text-balance text-white md:text-[34px]">
        Todo lo que su institución necesita en gases medicinales
      </h2>
      <p class="mt-3 text-[15.5px] text-white/75">
        Desde el diseño hasta la verificación final, cubrimos cada etapa bajo normativa ASSE y NFPA.
      </p>
    </div>

    <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
      {
        items.map((s, i) => {
          const Icon = resolveServiceIcon(s.icon);
          const accent = accentFor(i);
          return (
            <div class="group rounded-2xl bg-white p-6 ring-1 ring-slate-200 transition-[transform,box-shadow] hover:shadow-lg hover:shadow-slate-200/60 motion-safe:hover:-translate-y-0.5">
              <div class="mb-3 flex items-center gap-3">
                <div
                  class="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl transition-transform motion-safe:group-hover:scale-105"
                  style={{ backgroundColor: `${accent}1a` }}
                  aria-hidden="true"
                >
                  <Icon size={20} color={accent} strokeWidth={2} aria-hidden="true" />
                </div>
                <h3 class="font-display text-ink min-w-0 text-[15.5px] font-bold text-balance">{s.title}</h3>
              </div>
              <p class="text-[13.5px] leading-relaxed text-slate-600">{s.description}</p>
            </div>
          );
        })
      }
    </div>
  </div>
</section>
